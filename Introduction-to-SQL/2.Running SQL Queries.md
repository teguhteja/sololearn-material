# 2. Running SQL Queries

Think of your favorite app or technology. Chances are it wouldn’t run without data behind it.

In this lesson, you’ll write, test, and run real SQL code.

### Exercise: Extract Year of Release from the Movies Table

Complete the SQL query to select the release year from the `movies` table.

```sql
SELECT year
FROM movies;
```

### Understanding Structured Data

Data that can be stored in tables is known as **structured data**.

**Question:**  
*The data shown in the image above is...*

- ✅ structured data
- untidy data

SQL makes working with structured data fast and effective. A single query can retrieve thousands of records in seconds.

### Exercise: Complete the Query to Retrieve Year Data

```sql
SELECT year
FROM movies;
```

### The Code Playground

Ready to write, run, and test real code? Open the **Code Playground** and hit "Run" to get the requested data.

**Sample Output:**

```plaintext
year
1990
1977
1993
2013
2003
(5 rows)
```

This query results in a smaller table with only the requested data.

### It’s Coding Time!

Did you notice that you could modify the code? Now, try changing it!

**Exercise:**  
Can you extract the `title` field instead?

```sql
SELECT title
FROM movies;
```

Real databases can contain vast amounts of information.

**Question:**  
*A query is a way to...*

- make a copy of the entire database
- ✅ access only the data you are interested in

### Exercise: Complete the Code to Extract Data

```sql
SELECT year
FROM movies;
```

**Question:**  
*Look at the image above. Which data will this query extract?*

- Data highlighted in 1
- ✅ Data highlighted in 2

### Structured vs. Unstructured Data

**Unstructured data** is information that cannot be easily stored in tables.

**Match the data with its category:**

- **Sales table:** structured data
- **Audio file:** unstructured data

SQL stands for **Structured Query Language**. With SQL, you’ll be able to extract data from massive datasets with thousands of fields and records.

**Question:**  
*SQL is used to work with…*

- sound waves in audio files
- ✅ structured data in the form of tables

## Lesson Takeaways

Well done! You learned that:

- ⭐ Data can come from different sources.
- ⭐ Data can be classified as structured or unstructured.

In the next lesson, you’ll learn how databases are organized and work with data from multiple tables.
