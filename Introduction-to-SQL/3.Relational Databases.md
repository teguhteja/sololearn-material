# 3.Relational Databases

Companies and organizations of all sizes and industries extract useful insights by connecting different pieces of related data.

 

In this lesson, you’ll learn how related data is organized in databases.

The relational database is the most common type of database. The image shows a relational database.

A relational database…

can only store 1 data table
can contain several tables

The different tables in a relational database connect to each other using fields (columns) with values in common. These fields are called keys.

Use the keys to find which studios produced these movies

Star Wars:
Frozen:



Translate course
A relational database stores data in tables. 

 

What data category can a relational database handle?

Unstructured data
Structured data

Place the number in the correct gap

tables in the database:
records in the movies table:
fields in the studios table:

Key fields are used to connect the tables in a relational database.

Which field in the movies table is used to connect to the studios table?

title
studio_id


Translate course
The different tables in a relational database have unique names. The name of the table you want to access data FROM needs to be included in the SQL query.

SELECT year_founded
FROM studios

You need to make sure you are extracting fields from the correct table.

Complete the SQL query to get the studio names

SELECT name
FROM

Use these tables' key fields to find which item each customer ordered

John:
Jane:

You might have used spreadsheets like Excel to work with data before. Databases are better for storing larger and more complex collections of data.

Choose the right tool for the situation

Working with data in 1 single table:
Working with data in multiple, larger tables:

You can select multiple fields from one table at once by separating them with commas.

title
year
FROM movies


Translate course
Which 2 fields are being extracted with the following SQL query?

SELECT name, price
FROM products
SQL
